docker build .
docker rm testing2
docker run --name testing2 -dp 8080:80 nginxtestimage

dentro de nginx:
openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/ssl/private/nginx-selfsigned.key -out /etc/ssl/certs/nginx-selfsigned.crt